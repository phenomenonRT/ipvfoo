# IPvFoo Collector Server

Локальный сервер для сохранения пар домен+IP, которые фиксирует расширение IPvFoo.

## Требования

- Node.js 18+

## Установка и запуск

```bash
cd server
npm install
npm start
```

Сервер запустится на `http://127.0.0.1:3456`.

Чтобы использовать другой порт:

```bash
node server.js 8080
```

## Веб-интерфейс

Откройте в браузере: **http://127.0.0.1:3456**

Там можно:
- смотреть все уникальные пары домен+IP
- просматривать историю визитов
- искать по домену или IP
- видеть статистику (записей, визитов, уникальных IP)

## API

| Метод | URL | Описание |
|-------|-----|----------|
| `POST` | `/api/collect` | Принять записи от расширения |
| `GET`  | `/api/entries` | Уникальные пары домен+IP |
| `GET`  | `/api/visits`  | История всех визитов |
| `GET`  | `/api/stats`   | Краткая статистика |

Параметры для `/api/entries` и `/api/visits`: `page`, `limit`, `search`.

## Настройка в расширении

1. Запустите сервер (`npm start`)
2. Откройте настройки расширения IPvFoo (иконка → Options)
3. В секции **«Сервер сбора данных»** включите переключатель
4. При необходимости измените адрес (по умолчанию `http://127.0.0.1:3456`)
5. Нажмите **«Проверить»** — должно появиться подтверждение связи

## Данные

База данных SQLite сохраняется в `server/ipvfoo.db`. Файл создаётся автоматически при первом запуске.
